using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChurnTimer : MonoBehaviour
{
    /*
     * Press Begin
     * Begin turns to "Grab Churner"
     * Grab Churner
     * Get Ready!
     * Countdown from 3 with hand engaged. If person lets go, reset
     * At 3, "GO" start 30 second timer
     * Print score
     * 
     */
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    
    
    
}
